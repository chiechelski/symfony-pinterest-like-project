imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

    - { resource: '@PinterestLikeCoreBundle/Resources/config/services.yml' }

framework:
    #esi:             ~
    #translator:      { fallbacks: ["%locale%"] }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [ PinterestLikeStaticBundle, PinterestLikeUserBundle ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: "%kernel.root_dir%/Resources/java/compiler.jar"
        #yui_css:
        #    jar: "%kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar"
    assets:
        jquery_file_upload:
            inputs:

# Doctrine Configuration
doctrine:
    dbal:
        driver:   "%database_driver%"
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/data/data.db3"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #     path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        auto_mapping: true
        mappings:
            translatable:
                type: annotation
                alias: Gedmo
                prefix: Gedmo\Translatable\Entity
                # make sure vendor library location is correct
                dir: "%kernel.root_dir%/../vendor/gedmo/doctrine-extensions/lib/Gedmo/Translatable/Entity"

doctrine_migrations:
    dir_name: %kernel.root_dir%/DoctrineMigrations
    namespace: Application\Migrations
    table_name: migration_versions
    name: Application Migrations

sonata_block:
    default_contexts: [cms]
    blocks:
        # Enable the SonataAdminBundle block
        sonata.admin.block.admin_list:
            contexts:   [admin]

hwi_oauth:
    #this is my custom user provider, created from FOSUBUserProvider - will manage the
    #automatic user registration on your site, with data from the provider (facebook. google, etc.)
    #and also, the connecting part (get the token and the user_id)
    connect:
        account_connector: my_user_provider
    # name of the firewall in which this bundle is active, this setting MUST be set
    firewall_name: main
    fosub:
        username_iterations: 30
        properties:
            # these properties will be used/redefined later in the custom FOSUBUserProvider service.
            facebook: 1397108077276725
            google: google_id
    resource_owners:
        facebook:
            type:                facebook
            client_id:           "1397106410610225"
            client_secret:       "dad0a6c737d4a71fb54982d19fdc2f83"
            scope:               "email"
            options:
                display: popup #dialog is optimized for popup window
        google:
            type:                google
            client_id:           "469994775960-qqjm206pnsgkgk4f2o0dmoq4em5npnr1.apps.googleusercontent.com"
            client_secret:       "tHHUoDt9_qvgq9BPGQDdSpD0"
            scope:               "email profile"
        yahoo:
            type:                yahoo
            client_id:           "dj0yJmk9enBCa0tNQ0R1c2JNJmQ9WVdrOVltUTFjV1JQTm5FbWNHbzlNQS0tJnM9Y29uc3VtZXJzZWNyZXQmeD05YQ--"
            client_secret:       "35dcfb281e922308e0a2e2be7d9d7aaf97cf646d"
    # here you will add one (or more) configurations for resource owners

oneup_uploader:
    mappings:
        gallery:
            frontend: blueimp # or any uploader you use in the frontend

fos_user:
    db_driver: orm
    firewall_name: main
    user_class: PinterestLike\UserBundle\Entity\User
    registration:
        form:
            type: PinterestLike_user_registration

services:
    hwi_oauth.user.provider.entity:
        class: HWI\Bundle\OAuthBundle\Security\Core\User\OAuthUserProvider

# Swiftmailer Configuration
swiftmailer:
    transport: "%mailer_transport%"
    host:      "%mailer_host%"
    username:  "%mailer_user%"
    password:  "%mailer_password%"
    spool:     { type: memory }

# you will get these parameters form https://code.google.com/apis/console/"
happy_r_google_api:
    application_name: PinterestLike
    oauth2_client_id: 469994775960-qqjm206pnsgkgk4f2o0dmoq4em5npnr1.apps.googleusercontent.com
    oauth2_client_secret: tHHUoDt9_qvgq9BPGQDdSpD0
    oauth2_redirect_uri: https://dev.PinterestLike.com/login/check-google
    developer_key: AIzaSyB2YYnYFdcWYKS2ng4xeis_Pb52J0B1FvU
    site_name: PinterestLike.com

liip_imagine:
    resolvers:
       default:
          web_path: ~

    filter_sets:
        cache: ~
        my_thumb:
            quality: 85
            filters:
                thumbnail: { size: [120, 120], mode: outbound }